div#function
  h2.heading Function
  div.body
    ol
      li CoffeeScript syntax:
        pre
          code.language-coffeescript
            | add = (x, y) -> x + y
      li Compile into JavaScript:
        pre
          code.language-javascript
            | var add;
            | 
            | add = function(x, y) {
            |   return x + y;
            | };